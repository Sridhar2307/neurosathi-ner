# Netlify Configuration for NeuroSathi NER
# Problem Statement: SIH26003 (Smart India Hackathon 2026)

[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

# Single Page App (SPA) redirect rule
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
